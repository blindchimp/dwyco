; negative: X25519-load with a garbage file must fail gracefully
gcompile(nl `print(|\n|)')
gcompile(printl x `print(<x>) nl()')
; (badhex.hex is created in the work dir by run_tests.sh)
try(X25519-load(badhex.hex) * prog(printl(NEG-OK)))